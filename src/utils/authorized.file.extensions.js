// Authorized image file extensions
const authorizedImageExtensions = ['.jpg', '.jpeg', '.png', '.gif', '.bmp', '.svg'];

// Authorized video file extensions
const authorizedVideoExtensions = ['.mp4', '.webm', '.ogv'];

module.exports = {
  authorizedImageExtensions,
  authorizedVideoExtensions
};